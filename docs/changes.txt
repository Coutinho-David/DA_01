Changes made to Graph.h


On the Vertex class:
    Changed addEdge(Vertex<T> *dest, double w1, double w2)
  
On the Edge class: 
    Changed constructor to handle 2 weights
    Changed parameters (added weight2)
    Changed getWeight() function

On Graph class:
    Changed addEdge function and addBidirectionalEdge
